import { FragmentProps } from "..";
import Wrapper from "../../utils/wrapper"

interface Measure3dModelProps extends FragmentProps {

}

const Measure3dModel = (props: Measure3dModelProps) => {
    return (
        <Wrapper title="检测三维图">
            检测三维图
        </Wrapper>
    )
};

export default Measure3dModel