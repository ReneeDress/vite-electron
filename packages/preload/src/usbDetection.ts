// import type { Device } from 'usb-detection';
// export type { Device } from 'usb-detection';
// const usbDetection = require('usb-detection');

// export const getUsbDevices = async () => {
//     return new Promise((resolve, reject) => {
//         usbDetection.find((err: any, devices: Device[]) => {
//             if (err) {
//                 reject(err);
//             } else {
//                 resolve(devices);
//             }
//         });
//     });
// };

